const argv = require("process").argv;
const comando = argv[2];

switch (comando) {
    case puedeJubilarse:
        let edad = +argv[3];
        let genero = argv [4];
        let aniosAporte = argv [5];
        
        break;

    default:
        break;
}